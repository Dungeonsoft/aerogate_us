using UnityEngine;
using System.Collections;

public class CursorLightScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
    void Update()
    {
        //GetComponent<UITexture>().color = new Color(1,1,1,Mathf.Sin(Time.time*2));

    }
}
